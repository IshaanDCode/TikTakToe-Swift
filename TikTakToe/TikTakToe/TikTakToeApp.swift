import SwiftUI

// Define the main entry point for the SwiftUI app
@main
struct TicTacToeSwiftUIApp: App {
    
    // The body property defines the main scene of the application
    var body: some Scene {
        // WindowGroup represents a window or a scene in the application
        WindowGroup {
            // ContentView is the main view of the application
            ContentView()
        }
    }
}
